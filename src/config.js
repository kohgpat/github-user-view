const config = {
  env: {
    CLIENT_ID: process.env.REACT_APP_CLIENT_ID,
    REDIRECT_URI: process.env.REACT_APP_REDIRECT_URI,
    GH_GATEKEEPER: process.env.REACT_APP_GH_GATEKEEPER
  }
};

export default config;
